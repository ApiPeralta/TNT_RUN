using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class Slider : MonoBehaviour
{
    Slider iceTimer;
    void Start()
    {
        iceTimer = GetComponent<Slider>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
